# Infrastructure only — no service containers.
# Tests boot the NestJS apps in-process via supertest.
# Run: docker compose -f docker-compose.test.yml up -d --wait

services:
  postgres-auth-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: auth_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - '5433:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U test -d auth_test']
      interval: 5s
      timeout: 3s
      retries: 10

  postgres-household-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: household_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - '5434:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U test -d household_test']
      interval: 5s
      timeout: 3s
      retries: 10

  postgres-transaction-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: transaction_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - '5435:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U test -d transaction_test']
      interval: 5s
      timeout: 3s
      retries: 10

  postgres-budget-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: budget_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - '5436:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U test -d budget_test']
      interval: 5s
      timeout: 3s
      retries: 10

  postgres-notification-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: notification_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - '5437:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U test -d notification_test']
      interval: 5s
      timeout: 3s
      retries: 10

  postgres-report-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: report_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - '5438:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U test -d report_test']
      interval: 5s
      timeout: 3s
      retries: 10

  postgres-audit-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: audit_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - '5439:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U test -d audit_test']
      interval: 5s
      timeout: 3s
      retries: 10

  rabbitmq-test:
    image: rabbitmq:3-management-alpine
    ports:
      - '5673:5672'
      - '15673:15672'
    healthcheck:
      test: ['CMD', 'rabbitmq-diagnostics', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
